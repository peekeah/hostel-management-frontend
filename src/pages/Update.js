import React from "react";
import { Sidebar } from "../components";

const Update = () => (
    <div className="update-container d-flex">
        <Sidebar />
        <div className="text-dark">Hello from update</div>
    </div>
);

export default Update;
